# ============================
# RAG 知识库系统 - 后端依赖
# ============================

# --- Web 框架 ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# --- 数据库 ---
sqlalchemy[asyncio]>=2.0.29
asyncpg>=0.29.0
alembic>=1.13.1

# --- 向量数据库 ---
chromadb>=0.5.0

# --- HTTP 客户端 ---
httpx>=0.27.0

# --- Token 计数 ---
tiktoken>=0.7.0

# --- 认证 ---
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
pydantic[email]>=2.7.0

# --- LangChain (核心架构，无 LangGraph，无 RAGAS) ---
langchain-core>=0.3.0
langchain-openai>=0.1.0    # MiMo/DeepSeek/GLM (OpenAI 兼容)
langchain-ollama>=0.2.0     # OllamaEmbeddings (本地 embedding)
langchain-experimental>=0.3.0  # SemanticChunker
langchain-text-splitters>=0.3.0  # RecursiveCharacterTextSplitter

# --- 本地模型（HF 离线加载 + BM25） ---
sentence-transformers>=2.7.0   # HF embedding + rerank (CrossEncoder)
rank_bm25>=0.2.2               # BM25 检索
jieba>=0.42.1                  # 中文分词（BM25）

# --- 评估（自实现，无 ragas/datasets）---
# 5 检索指标：Recall@K / Precision@K / Hit@K / MRR / NDCG@K（metrics.py 纯算法）
# 3 LLM 指标：Faithfulness / Answer Relevancy / Answer Correctness（llm_metrics.py + LangChain）
# LLM 评估模型：settings.MIMO_LITE_MODEL（.env 中可改）

# --- 记忆系统 ---
# Store: PostgreSQL JSONB (不需要额外依赖)

# --- 文档解析 ---
PyMuPDF>=1.24.0
python-docx>=1.1.0
openpyxl>=3.1.2
python-pptx>=0.6.23
beautifulsoup4>=4.12.3
lxml>=5.2.0

# --- 工具 ---
python-dotenv>=1.0.1
